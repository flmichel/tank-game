apiVersion: apps/v1
kind: Deployment
metadata:
  name: tank-game-server-deployment
  namespace: tank-game
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tank-game-server
  template:
    metadata:
      labels:
        app: tank-game-server
    spec:
      containers:
        - name: tank-game-server
          image: harbor.flmichel.duckdns.org/tank-game/server:latest
      imagePullSecrets:
        - name: docker-secret
